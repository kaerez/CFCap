{
  "name": "cfcap",
  "version": "1.0.0",
  "description": "CFCap - Cloudflare Worker for @cap.js/server",
  "main": "src/index.js",
  "scripts": {
    "start": "wrangler dev",
    "predeploy": "node scripts/update-config.js",
    "deploy": "npm run predeploy && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "author": "Erez Kalman",
  "license": "Commercial",
  "dependencies": {
    "@cap.js/server": "latest"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
